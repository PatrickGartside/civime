extends:
  - recommended

rules:
  # The Access100 OpenAPI spec uses nullable: true (OpenAPI 3.0 pattern) in a
  # 3.1.0 spec. We preserve the source structure verbatim and disable struct
  # validation to avoid false positives on this common migration pattern.
  struct: off
  info-license: off
